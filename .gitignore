# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# Fotos dos produtos: NÃO versionar. Em produção elas são baixadas pela IMPORTAÇÃO
# (Laquila -> public/uploads) e ficam num VOLUME PERSISTENTE do Coolify (/app/public/uploads).
/public/uploads/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — NUNCA versionar segredos (token da Laquila, etc.)
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# Planilhas de produtos (dados do fornecedor, nao versionar)
*.xlsx
